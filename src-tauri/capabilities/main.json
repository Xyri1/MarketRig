{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The one window's capability (feature SPEC §5). The four app commands are not listed: in Tauri 2 a capability gates plugin commands only, and commands registered through generate_handler! on the app itself are always reachable from the window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "notification:default",
    "autostart:default",
    "log:default"
  ]
}
